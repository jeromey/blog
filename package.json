{
  "name": "website",
  "version": "1.0.0",
  "description": "jeromesalimao.com",
  "scripts": {
    "clean": "rm -rf ./_site",
    "build": "env NODE_ENV=production npm run clean && npx eleventy --pathprefix 'blog' --formats=html,njk,css,png,gif", 
    "start": "eleventy --serve --watch   --formats=html,njk,css,png,gif"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0"
  }
}
